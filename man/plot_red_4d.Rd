% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_red_4d.R
\name{plot_red_4d}
\alias{plot_red_4d}
\title{Plot 4D red-listing classes as a strict 4x4 matrix}
\usage{
plot_red_4d(
  results,
  variety_col = "final_variety_name",
  palette = c(`Critically At Risk` = "#FF0000", `At Risk` = "#FFA500",
    `Potentially Vulnerable` = "#FFFF00", `Stable, Low Concern` = "#92D050", Secure =
    "#008000"),
  return_tables = FALSE
)
}
\arguments{
\item{results}{A data frame returned by \code{\link[=get_red_listing]{get_red_listing()}}.}

\item{variety_col}{String. Variety identifier column in \code{results} used to
count unique varieties (default: \code{"final_variety_name"}).}

\item{palette}{Named character vector with colors for \verb{Critically At Risk},
\verb{At Risk}, \verb{Potentially Vulnerable}, \verb{Stable, Low Concern}, and \code{Secure}.}

\item{return_tables}{Logical. If \code{FALSE} (default), returns only the plot.
If \code{TRUE}, returns a list with \code{plot}, \code{square_summary},
\code{square_sum_breakdown}, \code{square_metric_breakdown}, and
\code{variety_assignment}. For backward compatibility it also includes
\code{limiting_metric_table} and \code{limiting_metric_detail}.}
}
\value{
A ggplot object, or a list when \code{return_tables = TRUE}.
}
\description{
Build a 4x4 matrix plot from \code{\link[=get_red_listing]{get_red_listing()}} output.
}
\details{
Strict cell filling rule:
a variety contributes to cell \verb{(X, Y)} only when all of the following hold:
\itemize{
\item \code{RCF_scale_num == X}
\item \code{GDF_num == X}
\item \code{OCF_scale_num == Y}
\item \code{ADF_num == Y}
}

Cell colors follow the theoretical matrix bands from \code{cell_label = 2 * (X + Y)}:
\code{4}, \code{5-7}, \code{8-11}, \code{12-15}, \code{16}.
}
\examples{
data(Huancavelica_2013)
results <- get_red_listing(Huancavelica_2013)
p <- plot_red_4d(results)
print(p)
}
