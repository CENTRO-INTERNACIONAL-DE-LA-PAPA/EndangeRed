% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_risk_category_counts.R
\name{plot_risk_category_counts}
\alias{plot_risk_category_counts}
\title{Plot unique variety counts by risk category}
\usage{
plot_risk_category_counts(
  results,
  variety_col = "final_variety_name",
  risk_col = "risk_category",
  palette = c(`Critically At Risk` = "red", `At Risk` = "orange",
    `Potentially Vulnerable` = "yellow", `Stable, Low Concern` = "#8CD665", Secure =
    "#1E7124"),
  drop_empty = FALSE
)
}
\arguments{
\item{results}{A data frame returned by \code{\link[=get_red_listing]{get_red_listing()}}.}

\item{variety_col}{String. Variety identifier column in \code{results} used to
count unique varieties (default: \code{"final_variety_name"}).}

\item{risk_col}{String. Risk category column in \code{results}
(default: \code{"risk_category"}).}

\item{palette}{Named character vector with colors for risk categories.}

\item{drop_empty}{Logical. If \code{TRUE}, categories with zero counts are omitted.}
}
\value{
A ggplot object.
}
\description{
Build a bar chart with the number of unique varieties per risk category from
\code{\link[=get_red_listing]{get_red_listing()}} output.
}
\examples{
data(Huancavelica_2013)
results <- get_red_listing(Huancavelica_2013)
p <- plot_risk_category_counts(results)
print(p)
}
